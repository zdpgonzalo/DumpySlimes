class LevelGenerator {
    constructor()
    {
        this.blocks = [];
        let block1 = JSON.parse(tilemap1).layers[0].data;
        blocks.push(block1);
        //repetir
    }

    generateLevel()
    {
        //
    }
}